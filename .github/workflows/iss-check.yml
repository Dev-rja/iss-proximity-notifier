name: ISS Proximity Check

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Ping ISS API
        run: curl -s https://iss-proximity-notifier.vercel.app/api/iss